# SigilDERG Ecosystem Input Requirements
# ========================================
# This file is used by pip-tools to generate requirements.lock
#
# Usage:
#   1. Install pip-tools: pip install pip-tools
#   2. Generate lock: pip-compile requirements.in --output-file=requirements.lock
#   3. Install locked: pip install -r requirements.lock
#
# Or use uv (faster):
#   1. Install uv: pip install uv
#   2. Generate lock: uv pip compile requirements.in -o requirements.lock
#   3. Install locked: uv pip sync requirements.lock
#
# Generated: 2025-11-28
# Version: 1.0.0

# =============================================================================
# SIGILDERG ECOSYSTEM PACKAGES
# =============================================================================
# Install from PyPI (or GitHub fallback handled by sigilderg.sh)
human-eval-rust>=2.3.0
sigil-pipeline>=2.3.0
sigilderg-finetuner>=3.0.0

# =============================================================================
# PYTORCH (install separately with CUDA-specific index)
# =============================================================================
# NOTE: PyTorch must be installed BEFORE pip-compile/pip-sync
# because it requires special index URLs for CUDA support.
#
# Pre-install for CUDA 12.8+:
#   pip install torch==2.7.1+cu128 torchvision==0.22.1+cu128 torchaudio==2.7.1+cu128 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# The constraints below ensure pip doesn't try to upgrade/downgrade
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# =============================================================================
# SHARED DEPENDENCIES WITH PINNED COMPATIBLE VERSIONS
# =============================================================================
# These resolve conflicts between ecosystem packages

# Rich: sigil-pipeline requires <14.0.0, finetuner requires >=13.0.0
rich>=13.7.0,<14.0.0

# PSutil: sigil-pipeline requires <7.0.0
psutil>=6.1.1,<7.0.0

# Termcolor: ecosystem standard
termcolor>=3.2.0

# =============================================================================
# ML STACK
# =============================================================================
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.1
huggingface-hub>=0.24.0
datasets>=2.20.0
trl>=0.9.6
evaluate>=0.4.2

# =============================================================================
# DATA PROCESSING
# =============================================================================
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
jsonlines>=4.0.0
pyyaml>=6.0
protobuf>=3.20.0,<5.0.0

# =============================================================================
# UTILITIES
# =============================================================================
tqdm>=4.66.0,<5.0.0
pydantic>=2.5.0,<3.0.0
typer>=0.9.0
click>=8.1.0,<9.0.0
requests>=2.31.0,<3.0.0
aiohttp>=3.9.0,<4.0.0
